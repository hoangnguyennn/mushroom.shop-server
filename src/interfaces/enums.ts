export enum CollectionName {
  CATEGORY = 'categories',
  IMAGE = 'images',
  PRODUCT_UNIT = 'productUnits',
  USER = 'users'
}

export enum UserType {
  ADMIN = 'ADMIN',
  CUSTOMER = 'CUSTOMER',
  MANAGER = 'MANAGER'
}
