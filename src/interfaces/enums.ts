export enum CollectionName {
  CATEGORY = 'categories',
  PRODUCT_UNIT = 'productUnits',
  USER = 'users'
}

export enum UserType {
  ADMIN = 'ADMIN',
  CUSTOMER = 'CUSTOMER',
  MANAGER = 'MANAGER'
}
